# Include
include LICENSE
include README.md
include pyproject.toml

# Include package files
recursive-include canary_mlx *.py

# Exclude model files
exclude model/*
recursive-exclude model *

# Exclude test/conversion data
exclude other/*
recursive-exclude other *
exclude canary_untared/*
recursive-exclude canary_untared *
exclude *.safetensors
exclude *.ckpt
exclude *.nemo
exclude *.tar
exclude *.wav

# Exclude cache
global-exclude __pycache__
global-exclude *.py[cod]

